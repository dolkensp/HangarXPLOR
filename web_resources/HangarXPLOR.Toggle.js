
var HangarXPLOR = HangarXPLOR || {};

// Render a toggle that sets the value of an element
HangarXPLOR.Toggle = function(label, value, className)
{
  console.log('Rendering Toggle', label, value);
  
  var $toggle = null;
  
  // TODO: Implement Toggle
  
  return $toggle;
}
